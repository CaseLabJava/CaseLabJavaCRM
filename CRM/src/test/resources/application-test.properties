spring.datasource.url=jdbc:h2:mem:testdb;MODE=MYSQL;DB_CLOSE_ON_EXIT=FALSE;DB_CLOSE_DELAY=-1;INIT=CREATE SCHEMA IF NOT EXISTS testdb\\;SET SCHEMA testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=test
spring.datasource.password=test
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update

MINIO_ACCESS=test12345;
MINIO_SECRET=test12345;
MINIO_URL=http://localhost:9091

spring.mail.username=${MAIL_USERNAME}
spring.mail.port=${MAIL_PORT:0}
spring.mail.password=${MAIL_PASSWORD}
