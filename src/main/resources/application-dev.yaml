spring:
  datasource:
    url: jdbc:postgresql://178.21.8.3:5432/
    name: postgres
    username: postgres
    password: postgres
  jpa:
    show-sql: true

kafka:
  url: localhost:9092

minio:
  access-key: root
  secret-key: root
  url: localhost:9000